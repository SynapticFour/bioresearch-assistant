version: "3.9"

services:
  postgres:
    image: pgvector/pgvector:pg16
    platform: ${DOCKER_PLATFORM:-linux/amd64}
    environment:
      POSTGRES_USER: ${DB_USER:-bioresearch}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-bioresearch}
      POSTGRES_DB: ${DB_NAME:-bioresearch}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U bioresearch"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    image: ${BACKEND_IMAGE:-ghcr.io/synapticfour/bioresearch-assistant-backend:latest}
    platform: ${DOCKER_PLATFORM:-linux/amd64}
    environment:
      DATABASE_URL: postgresql+asyncpg://${DB_USER:-bioresearch}:${DB_PASSWORD:-bioresearch}@postgres:5432/${DB_NAME:-bioresearch}
      PSEUDONYMIZATION_ENCRYPTION_KEY: ${PSEUDONYMIZATION_ENCRYPTION_KEY}
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY}
      SECRET_KEY: ${SECRET_KEY}
      ENVIRONMENT: production
      OLLAMA_BASE_URL: http://ollama:11434
      PUBMED_EMAIL: ${PUBMED_EMAIL}
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 4G        # Von 24GB — lässt Platz für Ollama
          cpus: '2'
    ports:
      - "8000:8000"

  frontend:
    image: ${FRONTEND_IMAGE:-ghcr.io/synapticfour/bioresearch-assistant-frontend:latest}
    platform: ${DOCKER_PLATFORM:-linux/amd64}
    depends_on:
      - backend
    restart: unless-stopped
    ports:
      - "3000:80"
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: '0.5'

  ollama:
    image: ollama/ollama:latest
    platform: ${DOCKER_PLATFORM:-linux/amd64}
    volumes:
      - ollama_data:/root/.ollama
    restart: unless-stopped
    ports:
      - "11434:11434"
    deploy:
      resources:
        limits:
          memory: 16G       # Großzügig — ARM hat 24GB total
          cpus: '1.5'

volumes:
  postgres_data:
  ollama_data:
