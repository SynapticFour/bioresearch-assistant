# GA4GH API Implementations

This directory contains GA4GH-compliant API implementations (e.g. DRS, WES, Phenopackets) for the BioResearch Assistant. Endpoints are exposed under `/ga4gh/` prefix by the backend.
