diff --git a/helixtest/crates/common/src/http.rs b/helixtest/crates/common/src/http.rs
--- a/helixtest/crates/common/src/http.rs
+++ b/helixtest/crates/common/src/http.rs
@@ -42,6 +42,20 @@ impl HttpClient {
         &self.inner
     }

+    fn attach_default_bearer(
+        &self,
+        builder: reqwest::RequestBuilder,
+    ) -> reqwest::RequestBuilder {
+        match std::env::var("HELIXTEST_DEFAULT_BEARER") {
+            Ok(token) if !token.trim().is_empty() => builder.bearer_auth(token.trim()),
+            _ => builder,
+        }
+    }
+
+    pub fn get_builder(&self, url: &str) -> reqwest::RequestBuilder {
+        self.attach_default_bearer(self.inner.get(url))
+    }
+
     #[instrument(skip(self))]
     pub async fn get_json(&self, url: &str) -> Result<serde_json::Value> {
         let resp = self.get_with_retry(url).await?;
@@ -76,9 +90,11 @@ impl HttpClient {
             "POST request"
         );
         let resp = self
-            .inner
-            .post(url)
-            .header("Content-Type", "application/json")
+            .attach_default_bearer(
+                self.inner
+                    .post(url)
+                    .header("Content-Type", "application/json"),
+            )
             .body(body_str)
             .send()
             .await?;
@@ -109,7 +125,7 @@ impl HttpClient {
         let strategy = ExponentialBackoff::from_millis(200)
             .map(jitter)
             .take(GET_RETRY_ATTEMPTS);
-        let resp = Retry::spawn(strategy, || async { self.inner.get(url).send().await }).await?;
+        let resp = Retry::spawn(strategy, || async { self.get_builder(url).send().await }).await?;
         Ok(resp)
     }
 }
diff --git a/helixtest/crates/framework/src/drs.rs b/helixtest/crates/framework/src/drs.rs
--- a/helixtest/crates/framework/src/drs.rs
+++ b/helixtest/crates/framework/src/drs.rs
@@ -40,7 +40,7 @@ async fn level0_reachable(cfg: &TestConfig, client: &HttpClient) -> TestCaseResu
     );
     level0_http(
         "DRS object endpoint reachable",
-        client.inner().get(&url).send().await,
+        client.get_builder(&url).send().await,
     )
 }

@@ -147,7 +147,7 @@ async fn level2_checksum_correctness(
             .ok_or_else(|| anyhow::anyhow!("sha256 checksum entry missing checksum field"))?;

         let access_url = first_access_url(&v)?;
-        let resp = client.inner().get(access_url).send().await?;
+        let resp = client.get_builder(access_url).send().await?;
         if !resp.status().is_success() {
             anyhow::bail!(
                 "Failed to download DRS object for checksum validation: {}",
@@ -232,8 +232,7 @@ async fn level2_range_request(cfg: &TestConfig, client: &HttpClient) -> TestCase
         let access_url = first_access_url(&v)?;

         let resp = client
-            .inner()
-            .get(access_url)
+            .get_builder(access_url)
             .header("Range", "bytes=0-1023")
             .send()
             .await?;
diff --git a/helixtest/crates/framework/src/wes.rs b/helixtest/crates/framework/src/wes.rs
--- a/helixtest/crates/framework/src/wes.rs
+++ b/helixtest/crates/framework/src/wes.rs
@@ -41,8 +41,8 @@ async fn level0_service_info_reachable(cfg: &TestConfig, client: &HttpClient) ->
     );
     level0_http(
         "WES service-info reachable",
-        client.inner().get(&url).send().await,
+        client.get_builder(&url).send().await,
     )
 }

 async fn level1_service_info_schema(cfg: &TestConfig, client: &HttpClient) -> TestCaseResult {
