# Canonical install lockfiles are uv.lock and requirements.txt.
# This Poetry lock is an empty stub (package = []) and is not used to install.
[project]
name = "bioresearch-assistant-backend"
version = "0.1.0"
description = "BioResearch Assistant API"
requires-python = ">=3.11,<3.13"

[tool.pytest.ini_options]
asyncio_mode = "auto"
addopts = "--cov=app --cov-report=term-missing --cov-report=xml --cov-fail-under=72"
