# BioResearch Assistant — Backend Dependencies
# Python 3.11+
--extra-index-url https://download.pytorch.org/whl/cpu

# Web
fastapi>=0.109.0,<1.0
python-multipart>=0.0.6,<1.0
uvicorn[standard]>=0.27.0,<1.0

# Database
sqlalchemy[asyncio]>=2.0.52,<3.0
asyncpg>=0.31.0,<1.0
psycopg2-binary>=2.9.0,<3.0
pgvector>=0.2.4,<1.0

# Config & validation
pydantic>=2.5.0,<3.0
pydantic-settings>=2.1.0,<3.0

# Migrations
alembic>=1.19.1,<2.0

# Utilities
httpx>=0.26.0,<1.0
slowapi>=0.1.9,<1.0

# OAuth2 / OIDC (GA4GH Passport) — PyJWT uses cryptography, not python-ecdsa
PyJWT[crypto]>=2.10.0,<3.0
authlib>=1.2.0,<2.0

# Pseudonymization (DSGVO)
# Pin spacy so thinc gets pre-built wheels (no C compiler required)
spacy==3.7.4
presidio-analyzer>=2.2.0,<3.0
# 2.2.364 caps cryptography<49; 2.2.362 allows cryptography 50 (PKCS#7 oracle fix).
presidio-anonymizer==2.2.362
cryptography>=50.0.0
# spaCy models for Presidio (de + en, small, pinned versions)
de_core_news_sm @ https://github.com/explosion/spacy-models/releases/download/de_core_news_sm-3.7.0/de_core_news_sm-3.7.0-py3-none-any.whl
en_core_web_sm @ https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.7.0/en_core_web_sm-3.7.0-py3-none-any.whl

# GA4GH Phenopackets v2
phenopackets>=2.0.0,<3.0

# LLM
anthropic>=0.39.0,<1.0

# Embeddings (local)
sentence-transformers>=2.2.0,<3.0
# Security floor (pip-audit on the hashed lock)
requests>=2.32.4
urllib3>=2.5.0,<3
idna>=3.7
certifi>=2024.7.4
setuptools>=78.1.1
# Linux CI/Docker: CPU torch (no CUDA toolkit). Other platforms: default PyPI wheel.
torch==2.13.0 ; sys_platform != "linux"
torch==2.13.0+cpu ; sys_platform == "linux"

# Bioinformatik (BLAST XML parsing)
biopython>=1.81,<2.0

# Tests (SQLite in-memory — no PostgreSQL needed)
aiosqlite>=0.22.1,<1.0
coverage
genbadge[coverage]
pytest>=9.0.3,<10.0
pytest-asyncio>=1.3.0,<2.0
pytest-cov>=4.1.0,<8.0
pytest-mock>=3.15.1,<4.0
